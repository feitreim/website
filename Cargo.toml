[package]
name = "website"
version = "0.1.0"
edition = "2021"

[dependencies]
pulldown-cmark = "0.12"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util", "fs"] }

[profile.release]
opt-level = 3
